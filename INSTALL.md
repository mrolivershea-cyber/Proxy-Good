# 🚀 Быстрая установка Tor-SOCKS Farm

## Автоматическая установка одной командой

На вашем Ubuntu сервере выполните:

```bash
wget https://raw.githubusercontent.com/mrolivershea-cyber/Proxy-Good/copilot/update-documentation-for-project/quick-install.sh
sudo bash quick-install.sh
```

Эта команда:
- ✅ Скачает все файлы проекта
- ✅ Установит необходимые пакеты (tor, 3proxy, netcat, jq)
- ✅ Создаст системных пользователей
- ✅ Скопирует файлы в /opt/tor-socks-farm/
- ✅ Покажет следующие шаги

## После установки

Выполните команды по порядку:

```bash
# 1. Системная настройка
sudo bash /opt/tor-socks-farm/scripts/install.sh

# 2. Развёртывание 50 Tor-нод
sudo bash /opt/tor-socks-farm/scripts/deploy_tor_instances.sh 50

# 3. Развёртывание прокси-эндпоинтов
sudo bash /opt/tor-socks-farm/scripts/deploy_3proxy_endpoints.sh 50

# 4. Включение автоматической ротации IP
sudo bash /opt/tor-socks-farm/scripts/enable_rotation.sh

# 5. Применение правил безопасности
sudo bash /opt/tor-socks-farm/firewall/hardening.sh
```

## Проверка работы

```bash
# Проверить статус служб
sudo systemctl list-units 'tor@*' 'troxy@*'

# Проверить подключение через прокси
curl -x socks5h://user001:pass001@localhost:20001 https://api.ipify.org
```

## Что будет установлено?

- **50 Tor-нод** с SOCKS5 прокси
- **50 публичных эндпоинтов** на портах 20001-20050
- **Автоматическая ротация IP** каждые 30 минут
- **Изоляция DNS** и отключение IPv6
- **Аутентификация** для каждого прокси

## Требования

- Ubuntu 22.04 LTS / 24.04 LTS
- Минимум 2GB RAM, 2 CPU, 10GB диск
- Root доступ (sudo)

## Порты

- **Локальные SOCKS:** 9001-9050
- **Control порты:** 9101-9150  
- **Публичные SOCKS:** 20001-20050

## Логины по умолчанию

```
Порт 20001: user001 / pass001
Порт 20002: user002 / pass002
...
Порт 20050: user050 / pass050
```

⚠️ **ВАЖНО:** Для production смените пароли на надёжные!

## Документация

После установки полная документация будет в:
- `/tmp/Proxy-Good-copilot-update-documentation-for-project/README.md`
- `/tmp/Proxy-Good-copilot-update-documentation-for-project/DEPLOYMENT_GUIDE.md`

## Помощь

Если возникли проблемы:
```bash
# Проверить логи
sudo journalctl -u tor@001
sudo journalctl -u 3proxy@001

# Статус служб
sudo systemctl status tor@001
sudo systemctl status 3proxy@001
```

---

**Версия:** 1.0.0  
**Лицензия:** MIT
